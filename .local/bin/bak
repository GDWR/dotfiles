#!/usr/bin/env xonsh
"""
Backup a file, creating a copy with the extension '.bak'
"""


def bak(file: str) -> None:
    $(cp @(file) @(file).bak)

if __name__ == "__main__":
    _, *args = $ARGS
    bak(*args)

